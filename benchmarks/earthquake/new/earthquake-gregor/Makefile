include ../Makefile

LAB_HOME=$(realpath ../notebooks)
IMAGE=nv-tf-21-05
TAG=latest
RAID=$(realpath /raid)
NOTEBOOKDIR=$(realpath ../notebooks)
PORT=8889
DOCKERFILE_DIR=..
OPTIONS=--gpus all --shm-size=1g --ulimit memlock=-1 --memory="16g"

# --memory-swap="16g" 
